var x;
var y;

// with function call, only registers

function main() {
  var res;

  x = 1;
  y = 2;
  res = 3 * y + add(x, y);
  putchar(48+res);
  // prints '9'
}

function add(x, y){
  return x + y;
}

