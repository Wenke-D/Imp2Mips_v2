var retour;
var big_a;

function main(arg) {
  var i;
  var small_v;

  i = 0;

  retour = 10;

  // this is small enough for registers
  small_v = test(2, 3, 4) + 99;

  // this expr is too much for registers
  // so, need stack to hold intermediate values
  big_a = test(2, 100, 300) + 100 + retour + retour + big_a + 10 + 100 + 9;
}

function test(x, y, z){
  var y;
  return 53 + retour + x;
}

